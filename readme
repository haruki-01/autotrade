# autotrade

Bybit BTC-USDT の自動売買 bot（研究〜実運用）。

## いまの方針

- 1本の bot で「仮説 → バックテスト → デモ → 少額本番」を通す
- UI なし（ログと数値で管理）
- 時間足: **日足=全体トレンド / 4時間=現在トレンド / 15分=エントリー**

## セットアップ

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## よく使うコマンド

```bash
# 実データ取得（ローカルPCなど Bybit API が使える環境で）
autotrade fetch-data --set all

# 本検証セット B でバックテスト
autotrade backtest --set B

# クラウド等で API が地理制限のとき（動作確認用・成績は参考にしない）
autotrade backtest --set B --synthetic
```

結果は `artifacts/backtests/` に出力されます。

> 注意: Cursor Cloud エージェント環境からは Bybit API が国制限で 403 になることがあります。  
> **本番相当の検証は、手元の Cursor ウィンドウで `fetch-data` → `backtest` を実行してください。**

## ドキュメント

- [全体プラン](docs/PROJECT_PLAN.md)
- [実装プラン（現状と次タスク）](docs/IMPLEMENTATION_PLAN.md)
- [戦略仮説 v1](docs/HYPOTHESIS.md)
- [バックテスト設計](docs/BACKTEST_DESIGN.md)
- [ローカルで別ウィンドウを開く](docs/LOCAL_DEV.md)
- [画面レイアウト（左フォルダ / 右チャット）](docs/LAYOUT.md)
- [Secrets（APIキー）](secrets/README.md)
