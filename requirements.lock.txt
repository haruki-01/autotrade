# Exact versions the recorded evaluation numbers were produced with.
#
# This file exists because it was needed. Rebuilding the VM produced a fresh
# set of packages, and 11 of 23 SH-01 variants stopped reproducing on
# byte-identical input data. The inputs were pinned by sha256 in the lock file;
# the code that read them was not pinned at all.
#
#   python3 -m pip install --user -r requirements.lock.txt
#
# Bumping anything here invalidates comparisons against older reports. Rerun
# the affected packs rather than reading the old numbers alongside new ones.

numpy==2.4.4
pandas==2.2.3
python-dateutil==2.9.0.post0
pytz==2026.3.post1
tzdata==2026.3

httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.2
certifi==2026.4.22
idna==3.13
sniffio==1.3.1

PyYAML==6.0.1
