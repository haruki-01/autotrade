# Demo / testnet keys. Open THIS file in the left Explorer (it is tracked so Cursor shows it).
# Do not put keys in *.example (those are committed to GitHub).
# Do not git add this file after filling keys. Run:
#   PYTHONPATH=src python3 -m autotrade.cli secrets-init

BYBIT_ENV=demo
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_BASE_URL=https://api-testnet.bybit.com
